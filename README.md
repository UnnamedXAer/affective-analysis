# Affective Analysis

# Run

```bash
# run the application
uv run python -m main

# run the application and save the log to a file
uv run python -m main > log.log 
```

## Testing

```bash
# run unit tests
uv run pytest -k "test_unit"

# run benchmark tests
uv run pytest -k "test_benchmark"
```
